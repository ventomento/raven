{
  "version": "1.0",

  "description": "Cryptographic configuration using X25519 key agreement and AES-256-GCM authenticated encryption",

  "standards": {
    "x25519": "RFC7748",
    "aesGcm": "NIST SP 800-38D",
    "sha256": "FIPS 180-4"
  },

  "identity": {
    "algorithm": "X25519",

    "curve": {
      "name": "Curve25519",
      "form": "Montgomery"
    },

    "privateKey": {
      "storedRepresentation": {
        "type": "32_byte_random_secret",
        "encoding": "hex",
        "byteOrder": "raw_byte_sequence"
      },

      "operationalRepresentation": {
        "type": "RFC7748_clamped_scalar",

        "clampingRules": [
          "clear bits 0,1,2 of byte 0",
          "clear bit 7 of byte 31",
          "set bit 6 of byte 31"
        ]
      },

      "generation": {
        "method": "cryptographically_secure_random_bytes",
        "sizeBytes": 32
      },

      "note": "The stored 32-byte secret is transformed into a clamped scalar during X25519 scalar multiplication operations"
    },

    "publicKey": {
      "representation": {
        "type": "montgomery_u_coordinate",
        "encoding": "hex",
        "sizeBytes": 32,
        "byteOrder": "little_endian"
      },

      "derivation": {
        "operation": "scalar_multiplication",
        "basePoint": "Curve25519_standard_basepoint_u_coordinate_9",
        "formula": "publicKey = X25519(privateKey, basePoint)"
      },

      "note": "The public key is the Montgomery u-coordinate only. The corresponding v-coordinate is intentionally omitted as specified by RFC 7748"
    },

    "sharedIdentity": {
      "type": "public_key",
      "field": "publicKey"
    }
  },

  "keyExchange": {
    "algorithm": "X25519",

    "sharedSecret": {
      "sizeBytes": 32,

      "representation": {
        "encoding": "hex",
        "byteOrder": "little_endian"
      },

      "formula": "sharedSecret = X25519(localPrivateKey, remotePublicKey)"
    },

    "securityProperties": [
      "Based on elliptic curve Diffie-Hellman over Curve25519",
      "Uses Montgomery ladder scalar multiplication",
      "Uses only the Montgomery u-coordinate representation",
      "Omission of the v-coordinate does not reduce practical discrete logarithm security",
      "Underlying elliptic curve points still form a mathematical group"
    ]
  },

  "symmetricEncryption": {
    "algorithm": "AES-GCM",

    "key": {
      "sizeBits": 256,

      "derivation": {
        "method": "SHA-256",
        "input": "raw_32_byte_x25519_shared_secret",
        "output": "32_byte_aes256_key",

        "formula": "aesKey = SHA256(sharedSecret)"
      }
    },

    "iv": {
      "sizeBytes": 12,
      "generation": "cryptographically_secure_random_bytes"
    },

    "authenticationTag": {
      "sizeBytes": 16
    }
  },

  "workflow": {
    "identityGeneration": [
      "1. Generate 32 cryptographically secure random bytes",
      "2. Store these bytes as the persistent private key secret",
      "3. Apply RFC7748 clamping during X25519 operations",
      "4. Compute publicKey = X25519(privateKey, basePoint)",
      "5. Share the resulting 32-byte public key"
    ],

    "encryptToRecipient": [
      "1. Obtain recipient X25519 public key",
      "2. Compute sharedSecret = X25519(localPrivateKey, recipientPublicKey)",
      "3. Compute aesKey = SHA256(sharedSecret)",
      "4. Generate random 12-byte IV",
      "5. Encrypt plaintext using AES-256-GCM"
    ],

    "decryptFromSender": [
      "1. Obtain sender X25519 public key",
      "2. Compute sharedSecret = X25519(localPrivateKey, senderPublicKey)",
      "3. Compute aesKey = SHA256(sharedSecret)",
      "4. Decrypt ciphertext using AES-256-GCM"
    ]
  },

  "notes": [
    "X25519 public keys are the primary participant identities",
    "No Ed25519 or Edwards25519 operations are used",
    "No curve conversions or birational mappings are used",
    "Public keys contain only Montgomery u-coordinates",
    "Private keys are stored as 32-byte random secrets",
    "Operational scalars are derived via RFC7748 clamping",
    "All key agreement operations follow RFC7748 exactly",
    "AES-GCM provides authenticated encryption and integrity protection",
    "A user need hex of public key to send messages to that address",
    "A user need hex of private key (seed) to 'log in' to his inbox"
  ]
}