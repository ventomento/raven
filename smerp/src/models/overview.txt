

  addQueueRelay(url) {

    this.queueRelays[url] = {
      enabled: true,
      lastSequenceId: null,
      lastSuccessAt: null,
      lastFailureAt: null,
      failureCount: 0,
    };
  }

  addArchiveRelay(url) {

    this.archiveRelays[url] = {
      enabled: true,
      sid: null, //lastSequenceId
      lastSuccessAt: null,
      lastFailureAt: null,
      failureCount: 0,
    };
  }


# relay record:
relayUrl
enabled
sid
lastSuccessAt
lastFailureAt
failureCount

# envelope record:
senderPublicKeyHex
recipientPublickeyHex
contentType
plaintext
timestamp
uuid
relayUrl,
receivedAt,

#conversation record:
publicKeyHex
unreadCount
lastMessageAt